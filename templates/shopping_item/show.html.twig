{% extends 'base.html.twig' %}

{% block title %}ShoppingItem{% endblock %}

{% block body %}
    <h1>ShoppingItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ shopping_item.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ shopping_item.label }}</td>
            </tr>
            <tr>
                <th>IsCheck</th>
                <td>{{ shopping_item.isCheck ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_shopping_item_index') }}">back to list</a>

    <a href="{{ path('app_shopping_item_edit', {'id': shopping_item.id}) }}">edit</a>

    {{ include('shopping_item/_delete_form.html.twig') }}
{% endblock %}
